"""srefix-prometheus-mcp: Prometheus query layer for the diagnosis stack.

Server entry point: ``prometheus_mcp.server:run`` (registered as ``srefix-prom``).
Pure-Python client (``prometheus_mcp.client``) is importable without the FastMCP
dependency for testing.
"""
