"""srefix-discovery-mcp: cluster auto-discovery for the diagnosis stack.

Server entry points live in ``srefix_discovery_mcp.server`` (which imports
``mcp``); pure-Python helpers (adapters / core) can be imported without
the FastMCP dependency.
"""
